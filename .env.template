# Environment Configuration Template

# 1. Server Configuration
# Set FLASK_ENV to 'production' for live deployments. Use 'development' for local testing.
FLASK_ENV=production
FLASK_APP=app.py
PORT=5000

# 2. Database
# For SQLite (default, saves to instance/attendance.db):
DATABASE_URL=sqlite:///attendance.db
# For PostgreSQL:
# DATABASE_URL=postgresql://user:password@localhost:5432/attendance_db

# 3. Security
# CRITICAL: You must generate a secure, random string (at least 32 chars) for production!
# You can generate one by running this command in your terminal:
# python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=replace_this_with_a_secure_random_string_of_at_least_32_chars
