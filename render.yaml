services:
  - type: web
    name: attendance-app
    env: python
    runtime: python3.11
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: gunicorn app:app
